<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WalkOfInterest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/green</item>
        <item name="fontFamily">@font/fira_sans_regular</item>
        <item name="android:fontFamily">@font/fira_sans_regular</item>
    </style>

    <style name="Theme.WalkOfInterest" parent="Base.Theme.WalkOfInterest" />
</resources>